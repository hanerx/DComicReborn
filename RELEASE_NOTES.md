<!--
按版本维护发布说明，最新版本放在最上方。
每个版本使用一级标题，例如 # 2.5.0；亮点、寄语等使用二级及以下标题。
正文可由维护者编写，也可由 AI 根据实际改动起草后审阅。
CI 只提取与发布标签匹配的版本正文，并追加自动生成的提交列表。
标签可以带 v 前缀；同一版本只保留一个章节，发布前必须补齐正文。
-->

# 2.5.2

## 阅读器优化

- 根据漫画源元数据识别条漫，进入阅读器时自动使用上下连续滚动；可在阅读设置中手动切换，条漫的方向选择仅影响当前阅读会话，不修改普通漫画的全局阅读方向。
- 修复纵向阅读中低分辨率图片放大时出现的页间黑缝，图片按容器宽度等比绘制并连续衔接，不裁剪原图。
- 纵向阅读在网络图片加载期间预留一屏高度，避免未加载页面挤成一排加载圈；加载后立即恢复图片实际比例，不留下额外页间空隙。

## 实验性功能

- 设置新增“实验性功能”页，与“调试设置”入口默认一起隐藏；在“关于”页点击顶部版本号 7 次后同时解锁，重启无需再次解锁。调试设置新增“重新隐藏调试与实验性功能”按钮，可持久隐藏两个入口并返回设置，之后仍可再次解锁。解锁和隐藏均不改变功能开关值，跨源“新”角标共享默认关闭。
- 开启后通过已有漫画绑定聚合最近查看时间，关联源的角标同步重新判定；不改写各源查看记录，关闭后恢复独立判定。
- 页面明确提示章节进度差异可能导致漏掉更新提示；显式解绑和不可靠的冲突关联不参与合并。
- 新增独立、默认关闭的“跨源聚合上次阅读进度”开关：已绑定漫画的历史章节能唯一对应时，按最后阅读时间统一详情页高亮和“继续阅读”；无法对应时各源独立，不覆盖原始历史或同步页码。

## 版本信息

- Android 构建号递增至 3。

# 2.5.1

## 宽屏选书与阅读

- 可用窗口宽度达到 840 个逻辑像素时，左侧浏览漫画，右侧显示详情与章节；首页保留顶部标签，不再常驻侧边导航。
- 首页、分类、排行、更新、搜索、收藏和历史统一使用详情面板；宽屏点击作者或分类时，在左侧展示结果并保留右侧漫画。
- 进入阅读后使用整个应用窗口，返回时恢复选书页面；宽窄窗口切换保留当前漫画与选书状态。

## 真机测试说明

- 已在 Pixel Tablet 模拟器验证双栏、单栏切换与阅读返回；折叠屏内外屏切换、铰链避让及半折叠姿态尚未完成专项验证。
- 展开后是否启用双栏取决于可用逻辑宽度，不取决于设备型号或物理分辨率。
- Android 构建号递增至 2，沿用现有发布签名。

# 2.5.0

## 本次亮点

### 漫画源体验补全

- **再漫画**：适配接口和登录态传递，修复已读收藏从书架消失的问题；新增云端历史查看和章节、页码同步，本地阅读记录仍独立保存。
- **自动签到**：再漫画登录或启动恢复有效登录态后，可自动完成当天签到；VIP 用户还可领取符合条件的每日福利。可在漫画源设置中关闭，失败不会让账号退出登录。
- **拷贝 / 热辣**：更新接口适配，漫画图片与吐槽可独立选择线路；首页刷新失败时保留原有内容，收藏按漫画更新时间倒序加载。

### 阅读与界面改进

- 章节末尾新增一屏吐槽预览，可继续翻页或打开完整评论。
- 阅读面板增加多种配色，工具栏、章末吐槽和设置弹层保持一致。
- 统一界面与登录按钮样式，完善漫画详情绑定、刷新和错误恢复交互；分类使用精选本地素材与统一比例。
- 优化收藏“新”角标更新，返回收藏页时保留已加载分页和滚动位置。
- 新增宽屏选书双栏：窗口达到 840 个逻辑像素时，左侧浏览漫画，右侧查看详情与章节；首页保留顶部标签，阅读时使用整个窗口，返回后恢复双栏。窄窗口自动切换为单页并保留当前漫画和选书状态。漫画网格动态调整，纵向阅读居中限宽，转屏时保持当前阅读页。

### 维护与修复

- 修复启动更新检查阻塞操作、首页漫画源恢复错位等问题。
- 修复更新提示覆盖渠道配置、导致“关于”页无法打开的问题；自动修复旧版本损坏的渠道配置，无法恢复原渠道时重置为稳定版。
- 升级至 Flutter 3.47.2 / Dart 3.13.2，更新依赖与平台构建配置，并迁移到 Floor 社区维护版本。
- 正式淘汰不再维护的 DMZJ provider，移除专属请求、协议和依赖，继续保留拷贝漫画与再漫画。

## 升级提示

- 最低支持 **Android 7.0 / API 24** 和 **iOS 15**。
- 不会自动删除旧源的本地历史、收藏或配置；DMZJ 不再提供访问能力。
- 再漫画云端同步需要有效登录态，不会自动批量上传已有的本地历史。
- iOS 发布附件为**未签名 IPA**，需要自行签名后安装。

## 寄语

这次更新主要围绕“漫画源能继续用、阅读与书架更顺手”展开：补齐再漫画的账号与历史能力，整理拷贝 / 热辣的接口适配，也为后续维护更新了底层工具链。

感谢一直使用和反馈问题的朋友。源站接口与网络环境仍会变化；如果遇到问题，欢迎附上应用版本、所用漫画源和复现步骤，帮助我们更准确地定位。
